/*
const guildSelect = document.querySelector('select');
openServerData(guildSelect.value);
guildSelect.onchange = () => openServerData(guildSelect.value);

function openServerData(serverID) {
  console.log(`Showing : ${serverID}`);
  document.getElementById(serverID).style.display = "flex";
}
*/