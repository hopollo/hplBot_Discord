import { GuildMember } from "discord.js";

export function voiceStateUpdate(oldState: GuildMember , newState: GuildMember) {
  //log("users_movements", msg, option);

  //deleteChannel.checkUsersOf(oldState);
}